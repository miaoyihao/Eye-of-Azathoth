# COC 人物卡向导 — 部署步骤

> 现有两套部署方式：
> - **线上部署**（推荐）：全球 CDN + 云端存储，供海外用户使用
> - **本地部署**：纯离线本地使用（旧方式）

---

# 线上部署（海外）

## 架构概览

```
浏览器 → Vercel (全球 CDN) → 前端静态页面
                         └→ Supabase (东京, PostgreSQL + Auth)
                         └→ Fly.io (东京, Python Excel 导出)
```

| 组件 | 用途 | 费用 |
|------|------|------|
| **Vercel** | 前端静态托管（全球 CDN，自动 HTTPS） | Hobby 免费（100GB 带宽/月） |
| **Supabase** | 数据库 + 用户认证（东京节点） | Free 免费（500MB DB / 5万 MAU） |
| **Fly.io** | Python Excel 导出后端（东京节点） | 免费额度内（绑卡） |

详细成本分析见对话历史记录，典型小工具月成本 **$0–30**。

## 前置准备

1. **GitHub**：已有 `github.com/miaoyihao/Eye-of-Azathoth`（test 分支已包含所有线上部署代码）
2. **Supabase**：在 https://supabase.com 创建项目，区域选 **Tokyo (ap-northeast-1)**
3. **Fly.io**（可选，Excel 导出需要）：在 https://fly.io 注册
4. （可选）自定义域名：暂用平台自带域名（`*.vercel.app` / `*.fly.dev`）

## 第一步 — 配置 Supabase

### 1.1 创建项目

在 Supabase Dashboard 创建新项目：
- **Name**: `coc-investigators`（任意）
- **Database Password**: 记下来
- **Region**: **Tokyo (ap-northeast-1)**
- **Pricing Plan**: Free

### 1.2 运行建表 SQL

创建完成后：
1. 进入 **SQL Editor**
2. 粘贴项目 `schema.sql` 的全部内容
3. 点击 **Run** 执行

### 1.3 配置认证（可选）

进入 **Authentication → Providers → Email**：
- 关闭 **Confirm email**（这样注册后无需验证邮件，可直接登录）

### 1.4 获取凭证

进入 **Project Settings → API**，复制以下两项：
- **Project URL**（`https://xxxxx.supabase.co`）
- **anon public key**

## 第二步 — 部署前端到 Vercel

1. 登录 https://vercel.com（用 GitHub 账号）
2. 点击 **Add New → Project**
3. 导入 `miaoyihao/Eye-of-Azathoth`
4. **Configure Project**：
   - **Root Directory**: 留空（根目录有 `vercel.json` 已配置好）
   - **Framework Preset**: Vite（自动识别）
   - **Environment Variables**（添加）：
     - `VITE_SUPABASE_URL` = 上面的 Project URL
     - `VITE_SUPABASE_ANON_KEY` = 上面的 anon key
     - `VITE_EXCEL_API_BASE` = 留空或填 Fly.io 地址（部署 Excel 后端后填写）
5. 点击 **Deploy**
6. 部署完成后收到 `https://eye-of-azathoth.vercel.app` 地址

> 当前 `test` 分支包含所有线上代码。在 Vercel 中可将 Production Branch 设为 `test`，
> 或合并到 `main` 后再部署。

## 第三步 — 部署 Excel 后端到 Fly.io（可选）

如果不需要 Excel 导出或接受前端 fallback（样式丢失），可跳过此步。

```bash
# 安装 flyctl
brew install flyctl

# 登录
fly auth login

# 创建应用（只需一次）
cd backend
fly launch --name coc-excel-export --region nrt --no-deploy

# 部署
fly deploy

# 部署后获取线上地址
fly status
# 输出中会显示 Hostname，如 coc-excel-export.fly.dev

# 在 Vercel 中更新 VITE_EXCEL_API_BASE = https://coc-excel-export.fly.dev
```

## 第四步 — 最终验证

1. 访问 Vercel 地址 → 看到「请先登录」页面 ✓
2. 点击「前往账号管理页」 → 进入 `/account`
3. 注册一个新账号 → 登录成功 ✓
4. 回到首页 → 创建人物卡 → 保存 ✓
5. 换浏览器/设备登录同一账号 → 数据同步 ✓
6. 导出 Excel → 模板样式完整 ✓

---

# 本地部署（旧方式）

## 前置条件

- 电脑已安装 **Git**（验证：`git --version`）
- 电脑已安装 **Node.js 18+**（验证：`node -v`）
- 未安装则从 https://nodejs.org 下载

## 完整步骤

### 第一步 — 从 Gitee 拉取项目

```bash
git clone https://gitee.com/miaoyihao/eye-of-azathoth.git
cd eye-of-azathoth
```

> 无需账号、无需密钥，公开仓库可直接克隆。

### 第二步 — 首次部署

```bash
bash deploy.sh
```

脚本会自动完成：
1. `npm install` — 安装依赖
2. `npm run build` — 构建前端
3. 启动本地服务

### 第三步 — 打开浏览器

访问 **http://localhost:3000** 即可使用 COC 人物卡向导。

## 后续使用

| 系统 | 操作 |
|------|------|
| **macOS** | 双击项目目录下的 `start.command` |
| **Windows** | 双击项目目录下的 `start.bat` |

## 更新到最新版

```bash
cd eye-of-azathoth
git pull
bash deploy.sh
```

## 数据备份与迁移

所有人物卡数据保存在 `data/characters.json`，复制此文件即可备份或迁移到其他电脑。

## 常见问题

### 权限错误（macOS）

如果双击 `start.command` 提示权限不足，在终端执行：

```bash
chmod +x start.command
```

### 页面空白

确保 Node.js 版本 ≥ 18，访问 http://localhost:3000 而不是双击 HTML 文件。

### Windows 注意事项

`start.bat` 运行后会打开一个命令行窗口，**请勿关闭**该窗口，关闭即停止服务。
