# Supabase 项目凭证（从 Supabase Dashboard → Settings → API 获取）
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Excel 导出后端地址（本地开发用 localhost，线上用 Fly.io 地址）
VITE_EXCEL_API_BASE=http://localhost:8080
