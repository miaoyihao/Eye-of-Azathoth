# COC 七版人物卡 · 字段说明书

> **版本** 1.0.0 | **最后更新** 2026-06-30 | **基准文件** `schema.sql` v2.0  
> **适用场景** 在线人物卡创建/编辑系统  
> **目标读者** 前端开发、后端开发、COC KP / 守秘人（审核规则正确性）

---

## 目录

1. [文档说明](#1-文档说明)
2. [区块一：基本信息](#2-区块一基本信息)
3. [区块二：九大属性](#3-区块二九大属性)
4. [区块三：自动派生值](#4-区块三自动派生值)
5. [区块四：点数系统](#5-区块四点数系统)
6. [区块五：技能表](#6-区块五技能表)
7. [区块六：武器表](#7-区块六武器表)
8. [区块七：护甲表](#8-区块七护甲表)
9. [区块八：叙事文本](#9-区块八叙事文本)
10. [区块九：法术表](#10-区块九法术表)
11. [区块十：伙伴/关系表](#11-区块十伙伴关系表)
12. [区块十一：疯狂/恐惧症/躁狂症](#12-区块十一疯狂恐惧症躁狂症)
13. [区块十二：状态标记](#13-区块十二状态标记)
14. [附加一：跨字段牵制关系总图](#14-附加一跨字段牵制关系总图)
15. [附加二：技能点完整分配流程](#15-附加二技能点完整分配流程)
16. [附加三：年龄减值表](#16-附加三年龄减值表)
17. [附加四：DB / Build 查表](#17-附加四-db--build-查表)
18. [附加五：信用评级范围速查](#18-附加五信用评级范围速查)
19. [附加六：技能基础值速查表](#19-附加六技能基础值速查表)
20. [附加七：点数完美分配判定（J50 规则）](#20-附加七点数完美分配判定j50-规则)

---

## 1. 文档说明

### 1.1 目的

本文档是 COC 七版人物卡在线表单的**唯一字段规格源（Single Source of Truth）**。所有字段的业务含义、数据类型、取值范围、输入组件、默认值、跨字段牵制关系均以本文档为准。

### 1.2 阅读约定

| 标记 | 含义 |
|------|------|
| 🔵 **必填** | 创建人物卡时必须填写 |
| 🟢 **推荐** | 建议填写但有合理默认值 |
| ⚪ **可选** | 可不填，留空合法 |
| 🔒 **只读** | 系统自动计算，用户不可编辑 |
| ✏️ **可编辑** | 用户直接输入或选择 |
| 🎲 **可掷骰** | 提供「骰」按钮随机生成 |

### 1.3 数据库约定

- 所有 ID 字段为 `INT AUTO_INCREMENT`
- 所有点数分配字段默认值为 `0`
- 所有百分比值存储为 0-99 整数（如 `25` 表示 25%）
- 所有半值/1/5值使用 `DIV` 整数除法（向下取整）
- 属性步进为 5（15/20/25/…/90）

---

## 2. 区块一：基本信息

### 2.1 姓名 `name`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.name` |
| 业务含义 | 调查员角色名 |
| 数据类型 | `VARCHAR(32)` |
| 取值范围 | 1-32 字符，无特殊字符限制 |
| 默认值 | — |
| 必填性 | 🔵 **必填** |
| 输入组件 | `<input type="text" maxlength="32">` |
| 牵制关系 | 无 |

### 2.2 玩家 `player`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.player` |
| 业务含义 | 玩家昵称或真实姓名 |
| 数据类型 | `VARCHAR(32)` |
| 取值范围 | 1-32 字符 |
| 默认值 | — |
| 必填性 | 🔵 **必填** |
| 输入组件 | `<input type="text" maxlength="32">` |
| 牵制关系 | 无 |

### 2.3 时代 `era`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.era` |
| 业务含义 | 故事发生的时代背景，决定可用职业、技能、装备范围 |
| 数据类型 | `VARCHAR(20)` |
| 取值范围 | 预设选项：`1920s`、`现代`、`1890s`、`煤气灯`、`1980s`、`冷战`、`中世纪`、`远古`、`未来` |
| 默认值 | `1920s` |
| 必填性 | 🔵 **必填** |
| 输入组件 | `<select>` 下拉选择 |
| 牵制关系 | → 影响可用职业列表（`occupation_templates.era` 过滤）  
→ 影响可用武器/装备（时代-发明对照）  
→ 1890s/煤气灯有独立职业扩展包 |

### 2.4 职业序号 `occupation_id`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.occupation_id` |
| 业务含义 | 从 230+ 职业模板中选择，`0` = 完全自定义 |
| 数据类型 | `INT` |
| 取值范围 | 0-230 |
| 默认值 | `0`（自定义） |
| 必填性 | 🔵 **必填** |
| 输入组件 | `<select>` 搜索下拉（按时代过滤），选 0 时展开自定义职业名输入框 |
| 牵制关系 | → 决定本职技能列表（`occupation_skills` 关联）  
→ 决定信用评级范围  
→ 决定职业点数公式（影响 `occupation_points_total`）  
→ 选 0 时 `occupation_name` 变为必填 |

### 2.5 职业名称 `occupation_name`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.occupation_name` |
| 业务含义 | 自定义职业的名称 |
| 数据类型 | `VARCHAR(64)` |
| 取值范围 | 1-64 字符 |
| 默认值 | `NULL` |
| 必填性 | ⚪ 可选（`occupation_id ≠ 0` 时隐藏）  
🔵 必填（`occupation_id = 0` 时） |
| 输入组件 | `<input type="text" maxlength="64">`，受 `occupation_id` 控制显隐 |
| 牵制关系 | ← 受 `occupation_id` 控制 |

### 2.6 年龄 `age`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.age` |
| 业务含义 | 调查员年龄 |
| 数据类型 | `TINYINT` |
| 取值范围 | 15-89（数据库 CHECK 约束），COC 规则中 15 为最低调查年龄 |
| 默认值 | `NULL` |
| 必填性 | 🟢 **推荐** |
| 输入组件 | `<input type="number" min="15" max="89">` |
| 牵制关系 | → **EDU 减值**：≥40 岁每满 10 年 EDU -5（见[附加三](#16-附加三年龄减值表)）  
→ **MOV 减值**：按年龄段减值（见[附加三](#16-附加三年龄减值表)）  
→ 40 岁起 UI 应显示「EDU:-5」提示 |

### 2.7 性别 `gender`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.gender` |
| 业务含义 | 调查员性别（无规则影响，纯 RP） |
| 数据类型 | `VARCHAR(16)` |
| 取值范围 | 自由文本 |
| 默认值 | `NULL` |
| 必填性 | ⚪ 可选 |
| 输入组件 | `<input type="text" maxlength="16">` 或 `<select>` 预设 |

### 2.8 住地 `residence`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.residence` |
| 业务含义 | 调查员当前居住地 |
| 数据类型 | `VARCHAR(128)` |
| 取值范围 | 自由文本 |
| 默认值 | `NULL` |
| 必填性 | ⚪ 可选 |
| 输入组件 | `<input type="text" maxlength="128">` |

### 2.9 故乡 `birthplace`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.birthplace` |
| 业务含义 | 调查员出生地 |
| 数据类型 | `VARCHAR(128)` |
| 取值范围 | 自由文本 |
| 默认值 | `NULL` |
| 必填性 | ⚪ 可选 |
| 输入组件 | `<input type="text" maxlength="128">` |

### 2.10 时间设定 `scenario_year` / `scenario_month` / `scenario_day`

| 属性 | scenario_year | scenario_month | scenario_day |
|------|--------------|----------------|--------------|
| 数据库列 | `investigators.scenario_year` | `investigators.scenario_month` | `investigators.scenario_day` |
| 业务含义 | 故事发生的年份 | 月份 | 日期 |
| 数据类型 | `SMALLINT` | `TINYINT` | `TINYINT` |
| 取值范围 | 正整数（如 1920） | 1-12 | 1-31 |
| 默认值 | `NULL` | `NULL` | `NULL` |
| 必填性 | ⚪ 可选 | ⚪ 可选 | ⚪ 可选 |
| 输入组件 | 三个紧凑 `<input number>` 或一个 `date-picker` |
| 牵制关系 | → 配合 `era` 共同约束可用内容 |

---

## 3. 区块二：九大属性

### 3.0 公共规则

- 所有属性值**必须是 5 的倍数**（COC 规则：3D6×5 / (2D6+6)×5）
- 推荐掷骰公式见各字段
- 属性值被 `DIV 2` 和 `DIV 5` 整数除法用于半值 / 1/5值
- 购点模式下：`SUM(9项属性) ≤ KP设定上限`（默认 460-480）

### 3.1 力量 STR `str_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.str_val` |
| 业务含义 | 肌肉力量，影响近战伤害、体格判定 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | 15-90（CHECK 约束），步进 5 |
| 推荐掷骰 | `3D6×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | `<input type="range" min="15" max="90" step="5">` + 数值显示 + 🎲 骰按钮 |
| 牵制关系 | → `str_half` = STR/2、`str_fifth` = STR/5  
→ **DB** = f(STR+SIZ)（查表）  
→ **Build** = f(STR+SIZ)（查表）  
→ 属性文字描述 |

### 3.2 敏捷 DEX `dex_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.dex_val` |
| 业务含义 | 身体协调性与反应速度，影响先攻、闪避技能基础值 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | 15-90，步进 5 |
| 推荐掷骰 | `3D6×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR |
| 牵制关系 | → `dex_half` = DEX/2、`dex_fifth` = DEX/5  
→ **闪避技能基础值 = DEX/2**  
→ MOV 减值计算 |

### 3.3 意志 POW `pow_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.pow_val` |
| 业务含义 | 精神力与意志力，决定初始 SAN 值和 MP 上限 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | 15-90，步进 5 |
| 推荐掷骰 | `3D6×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR |
| 牵制关系 | → `san_max = POW`（初始理智 = 意志值）  
→ `mp_max = POW / 5`  
→ `pow_half` / `pow_fifth` |

### 3.4 体质 CON `con_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.con_val` |
| 业务含义 | 健康与耐力，决定 HP 上限 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | 15-90，步进 5 |
| 推荐掷骰 | `3D6×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR |
| 牵制关系 | → `hp_max = (CON+SIZ) / 10`  
→ `con_half` / `con_fifth` |

### 3.5 外貌 APP `app_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.app_val` |
| 业务含义 | 外表吸引力，影响社交类技能 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | 15-90，步进 5 |
| 推荐掷骰 | `3D6×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR |
| 牵制关系 | → 部分职业公式包含 APP×2  
→ `app_half` / `app_fifth` |

### 3.6 教育 EDU `edu_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.edu_val` |
| 业务含义 | 受教育程度，决定职业点数池和母语基础值 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | 15-99（注意 CHECK 约束为 0-99，因年龄减值可能使结果低于 15），步进 5 |
| 推荐掷骰 | `(2D6+6)×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR，旁边显示「基础值」|「年龄修正后」两行 |
| 牵制关系 | → `occupation_points_total = EDU × 职业倍数`  
→ **母语技能基础值 = EDU**  
→ **年龄减值**：≥40 岁生效（见[附加三](#16-附加三年龄减值表)）  
→ `edu_half` / `edu_fifth` |

### 3.7 体型 SIZ `siz_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.siz_val` |
| 业务含义 | 身体体型与质量 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | **40**-90（CHECK 约束，成人下限 40），步进 5 |
| 推荐掷骰 | `(2D6+6)×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR |
| 牵制关系 | → `hp_max = (CON+SIZ) / 10`  
→ **DB** / **Build** = f(STR+SIZ)  
→ MOV 减值计算  
→ `siz_half` / `siz_fifth` |

### 3.8 智力 INT `int_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.int_val` |
| 业务含义 | 逻辑推理与学习能力，决定兴趣点数池 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | **40**-90（CHECK 约束，下限 40），步进 5 |
| 推荐掷骰 | `(2D6+6)×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR |
| 牵制关系 | → `interest_points_total = INT × 2`  
→ MOV 减值计算  
→ `int_half` / `int_fifth` |

### 3.9 幸运 LUCK `luck_val`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.luck_val` |
| 业务含义 | 运气值，可用于修改骰子结果（可选规则） |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | 15-99（CHECK 约束），步进 5 |
| 推荐掷骰 | `3D6×5` |
| 默认值 | `NULL` |
| 必填性 | 🔵 **必填** |
| 输入组件 | 同 STR |
| 牵制关系 | → `luck_half` / `luck_fifth`（可用于极难幸运检定） |

---

## 4. 区块三：自动派生值

> 以下字段为 🔒 **系统自动计算，用户只读**。前端应实时响应属性变化重新计算。

### 4.1 属性半值（9 个）

| 字段 | 公式 | 数据库实现 |
|------|------|-----------|
| `str_half` ~ `luck_half` | `属性值 DIV 2` | `GENERATED ALWAYS AS (val DIV 2) STORED` |

**业务含义**：困难难度检定时使用（成功率 × ½ 或属性半值对照）。

### 4.2 属性 1/5 值（9 个）

| 字段 | 公式 | 数据库实现 |
|------|------|-----------|
| `str_fifth` ~ `luck_fifth` | `属性值 DIV 5` | `GENERATED ALWAYS AS (val DIV 5) STORED` |

**业务含义**：极难难度检定时使用（成功率 × ⅕ 或属性 1/5 值对照）。

### 4.3 最大 HP `hp_max`

| 属性 | 值 |
|------|-----|
| 公式 | `(CON + SIZ) DIV 10` |
| 数据库实现 | `GENERATED ALWAYS AS ((con_val + siz_val) DIV 10) STORED` |
| 牵制 | ← CON + SIZ 任一变则 HP 重算 |

### 4.4 最大 SAN `san_max`

| 属性 | 值 |
|------|-----|
| 公式 | `= POW`（初始 SAN = 意志值） |
| 数据库实现 | `GENERATED ALWAYS AS (pow_val) STORED` |
| 牵制 | ← POW 变化则 SAN 重算 |

### 4.5 最大 MP `mp_max`

| 属性 | 值 |
|------|-----|
| 公式 | `POW DIV 5` |
| 数据库实现 | `GENERATED ALWAYS AS (pow_val DIV 5) STORED` |
| 牵制 | ← POW |

### 4.6 伤害加值 DB `db`

| 属性 | 值 |
|------|-----|
| 公式 | 查表 `f(STR + SIZ)`（见[附加四](#17-附加四-db--build-查表)） |
| 数据类型 | `VARCHAR(8)`，如 `-2`, `-1`, `0`, `+1D4`, `+1D6`, `+2D6`, `+3D6` |
| 计算方式 | 应用层查表（非数据库生成列） |
| 牵制 | ← STR + SIZ |

### 4.7 体格 Build `build`

| 属性 | 值 |
|------|-----|
| 公式 | 查表 `f(STR + SIZ)`（同 DB 查表） |
| 数据类型 | `TINYINT`，范围 -2 ~ 2+ |
| 计算方式 | 应用层查表 |
| 牵制 | ← STR + SIZ |

### 4.8 移动力 MOV `mov`

| 属性 | 值 |
|------|-----|
| 基础值 | `8` |
| 减值规则 | 1. **STR/SIZ差值**：STR > SIZ 或 SIZ > STR 时查减值表  
2. **DEX/SIZ差值**：同上  
3. **年龄减值**：见[附加三](#16-附加三年龄减值表)  
4. **护甲减值**：所有启用护甲的减值总和 |
| 最终公式 | `MOV = 8 - 减值1 - 减值2 - 年龄减值 - 护甲减值`（不低于某下限） |
| 计算方式 | 应用层 |

---

## 5. 区块四：点数系统

### 5.1 职业点数池 `occupation_points_total`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.occupation_points_total` |
| 业务含义 | 可用于分配职业点的总点数 |
| 公式 | `EDU × 职业倍数` |
| 职业倍数 | 由职业模板 `points_formula` 决定：  
• `EDU×4` → 会计师/医生/律师/教授  
• `EDU×2 + DEX×2` → 运动员/舞者  
• `EDU×2 + APP×2` → 演员/艺人  
• `EDU×2 + MAX(STR×2, DEX×2)` → 侦探/记者  
• `EDU×2 + MAX(DEX×2, APP×2)` → 流浪者/罪犯  
• 自定义职业倍数 = 4（默认） |
| 计算时机 | 选定职业 + 填写 EDU 后自动计算 |
| 牵制 | ← `occupation_id` + `edu_val` |

### 5.2 已用职业点 `occupation_points_used`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.occupation_points_used` |
| 业务含义 | 已分配到本职技能的职业点总和 |
| 公式 | `SUM(investigator_skills.occupation_pts WHERE is_occupation = TRUE)` |
| **校验规则** | `occupation_points_used ≤ occupation_points_total` |
| 前端实时校验 | 超出时标红，禁止提交 |

### 5.3 兴趣点数池 `interest_points_total`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.interest_points_total` |
| 业务含义 | 可用于分配兴趣点的总点数 |
| 公式 | `INT × 2` |
| 牵制 | ← `int_val` |

### 5.4 已用兴趣点 `interest_points_used`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.interest_points_used` |
| 业务含义 | 已分配到所有技能的兴趣点总和 |
| 公式 | `SUM(investigator_skills.interest_pts)` |
| **校验规则** | `interest_points_used ≤ interest_points_total` |
| 前端实时校验 | 超出时标红 |

### 5.5 经历包点数池 `experience_points_total`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.experience_points_total` |
| 业务含义 | 选择经历包后获得的额外技能点 |
| 来源 | 根据经历包类型预设（`战场`/`警务`/`罪犯`/`医务`/`神话`），由附表!D34 提供数值 |
| 激活条件 | 仅当 `experience_pack ≠ "无"` 时生效 |
| 牵制 | ← `experience_pack` |

### 5.6 已用经历包点 `experience_points_used`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.experience_points_used` |
| 业务含义 | 已分配的经历包点总和 |
| 公式 | `SUM(investigator_skills.experience_pts)` |
| **校验规则** | `experience_points_used ≤ experience_points_total` |
| **注意** | 经历包点通常只能分配到经历包对应的预设技能 |

---

## 6. 区块五：技能表

> 每项技能对应 `investigator_skills` 表的一行，每张人物卡约 55 行。

### 6.1 技能名 `skill_name`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.skill_name` |
| 业务含义 | COC 七版规则技能名称 |
| 数据类型 | `VARCHAR(32)` |
| 取值 | 来自预设技能列表（见[附加六](#19-附加六技能基础值速查表)），或自定义技能名 |
| 唯一性 | 同一人物卡内技能名唯一（`uk_inv_skill`） |
| 可编辑性 | 🔒 预设技能名只读；自定义技能可输入 |

### 6.2 基础值 `base_value`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.base_value` |
| 业务含义 | 技能初始成功率（不分配点数时的值） |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值 | 固定值（见[附加六](#19-附加六技能基础值速查表)，如 会计=5、图书馆=20、侦查=25、潜行=20）  
特例：闪避=DEX/2、母语=EDU、信用评级=0 |
| 可编辑性 | 🔒 只读（动态技能如闪避/母语在创建时根据属性计算） |

### 6.3 经历包点 `experience_pts`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.experience_pts` |
| 业务含义 | 从经历包获得的点数，分配到该技能 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | ≥ 0 |
| 默认值 | `0` |
| 可编辑性 | ✏️ 可选（仅 `experience_pack ≠ "无"` 时开放编辑），通常仅经历包预设技能可编辑 |
| **校验规则** | `SUM(experience_pts) ≤ experience_points_total` |

### 6.4 职业点 `occupation_pts`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.occupation_pts` |
| 业务含义 | 从职业点数池分配到此技能的点数 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | ≥ 0 |
| 默认值 | `0` |
| 可编辑性 | ✏️ 仅 `is_occupation = TRUE` 的技能可编辑（非本职灰掉） |
| **校验规则** | `SUM(occupation_pts WHERE is_occupation=TRUE) ≤ occupation_points_total` |

### 6.5 兴趣点 `interest_pts`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.interest_pts` |
| 业务含义 | 从兴趣点数池分配到此技能的点数 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | ≥ 0 |
| 默认值 | `0` |
| 可编辑性 | ✏️ 所有技能均可编辑 |
| **校验规则** | `SUM(interest_pts) ≤ interest_points_total` |

### 6.6 成长点 `growth_pts`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.growth_pts` |
| 业务含义 | 游戏中通过成长检定获得的点数 |
| 数据类型 | `TINYINT UNSIGNED` |
| 取值范围 | ≥ 0 |
| 默认值 | `0` |
| 可编辑性 | ✏️ **仅在「游戏进行中」模式下可编辑**（创建人物卡时固定为 0） |
| **业务规则** | 成长点不受任何点数池限制 |

### 6.7 本职标记 `is_occupation`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.is_occupation` |
| 业务含义 | 是否为本职技能（职业点只能分配到本职技能） |
| 数据类型 | `BOOLEAN` |
| 默认值 | `FALSE` |
| 可编辑性 | 🔒 预设职业自动填充  
✏️ 自定义职业（`occupation_id = 0`）时可手动勾选，最多 8 项 |
| 牵制关系 | → 控制 `occupation_pts` 是否可编辑 |

### 6.8 自定义技能名 `custom_name`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.custom_name` |
| 业务含义 | 自定义技能的具体名称（如「科学（天文学）」「外语（拉丁语）」） |
| 数据类型 | `VARCHAR(64)` |
| 默认值 | `NULL` |
| 可编辑性 | ✏️ 仅在预设技能名为通用名（科学/外语/自定义技能①②）时可输入 |

### 6.9 技能分类 `category`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.category` |
| 业务含义 | 技能大类，用于雷达图分组和筛选 |
| 取值 | `调查` / `交涉` / `战斗` / `特技` / `学识` / `支援` |
| 可编辑性 | 🔒 系统预设（根据技能名自动归类） |

### 6.10 成功率 `success_rate`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_skills.success_rate`（虚拟计算列） |
| 公式 | `base_value + experience_pts + occupation_pts + interest_pts + growth_pts` |
| 数据库实现 | `GENERATED ALWAYS AS (...) VIRTUAL` |
| 业务含义 | 普通难度检定的目标值 |
| 派生值 | • 困难成功率 = `success_rate DIV 2`  
• 极难成功率 = `success_rate DIV 5` |

### 6.11 技能表前端交互设计备注

```
┌────────────────────────────────────────────────────────────────────┐
│  🔍 [搜索技能...]     筛选: [全部▾] [本职▾] [战斗▾] [交涉▾]        │
│                                                                    │
│  ☑ 技能名      基础  经历包  职业  兴趣  成长  =  成功率   分类     │
│  ────────────────────────────────────────────────────────────────  │
│  ☑ 会计          5%  [  0]  [ 20]  [  0]  [  0]  =  25%    学识    │
│  ☑ 图书馆使用   20%  [  0]  [ 10]  [  0]  [  0]  =  30%    调查    │
│  ☐ 闪避       DEX/2  [  -]  [  -]  [ 30]  [  0]  =  60%    战斗    │
│  ☐ 聆听         20%  [  -]  [  -]  [ 20]  [  0]  =  40%    调查    │
│  ...                                                               │
│  ────────────────────────────────────────────────────────────────  │
│  经历包: 已用 0/10    职业点: 已用 60/80    兴趣点: 已用 50/60     │
│  [✅ 完美分配]  ← 三个池全部归零时显示，否则显示剩余提醒              │
└────────────────────────────────────────────────────────────────────┘

- 灰色 [-]: 非编辑状态（非本职技能的职业点、未选经历包的经历包点）
- 红色边框: 分配超出点数池上限
- ☑/☐: 本职技能标记（预设只读，自定义可勾选）
```

---

## 7. 区块六：武器表

> 每件武器对应 `investigator_weapons` 表的一行，动态增删。

### 7.1 武器名 `weapon_name`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_weapons.weapon_name` |
| 数据类型 | `VARCHAR(64)` |
| 必填性 | 🔵 **必填** |
| 输入组件 | `<input text>` 或 `<select>` 从武器库选（选预设自动填充伤害/射程等） |

### 7.2 使用技能 `skill_used`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_weapons.skill_used` |
| 数据类型 | `VARCHAR(32)` |
| 取值 | 斗殴 / 手枪 / 弓术 / 投掷 / 来复枪 / 冲锋枪 / 机枪 / 链枷 / 绞具 / 鞭子 / 电锯 / 斧 |
| 输入组件 | `<select>` |
| 牵制关系 | → 该武器的攻击检定使用此技能的成功率 |

### 7.3 伤害公式 `damage`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_weapons.damage` |
| 数据类型 | `VARCHAR(16)` |
| 格式 | `nDm` / `nDm+DB` / `nDm+半DB`（如 `1D4+DB`、`2D8`、`1D6+半DB`） |
| 牵制关系 | → DB 值变化时实际伤害需重新计算 |

### 7.4 攻击次数 / 装弹量 / 故障值 / 射程

| 字段 | 列 | 类型 | 默认值 | 说明 |
|------|-----|------|--------|------|
| 射程 | `attack_range` | `VARCHAR(16)` | — | 徒手 / 10码 / 25码 等 |
| 每轮攻击次数 | `attacks_per_round` | `TINYINT UNSIGNED` | `1` | 默认 1 |
| 装弹量 | `ammo` | `INT` | — | 枪械弹药 |
| 故障值 | `malfunction` | `TINYINT UNSIGNED` | — | 0-100，骰子出目≤此值时武器故障 |

### 7.5 临时武器 `is_improvised`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_weapons.is_improvised` |
| 业务含义 | 标记为临时武器（如椅子、瓶子），不享受常规武器熟练加成 |
| 输入组件 | `<checkbox>` |

---

## 8. 区块七：护甲表

> 每件护甲对应 `investigator_armors` 表的一行。

### 8.1 护甲名 `armor_name`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_armors.armor_name` |
| 数据类型 | `VARCHAR(64)` |
| 输入组件 | `<input text>` 或 `<select>` 从防具表选 |

### 8.2 护甲值 `protection`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_armors.protection` |
| 业务含义 | 护甲提供的伤害减免值 |
| 取值范围 | ≥ 0 |
| 牵制关系 | → 启用护甲计入 MOV 减值计算  

 ### 8.3 是否启用 `is_active`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigator_armors.is_active` |
| 业务含义 | 当前是否穿戴（脱下的护甲不计入 MOV 减值） |
| 默认值 | `TRUE` |
| 输入组件 | `<toggle>` |
| 牵制关系 | → `is_active = TRUE` 时才参与 MOV 减值计算 |

---

## 9. 区块八：叙事文本

> 全为大文本框（`<textarea>`），无硬性字符限制。

| 字段 | 数据库列 | 类型 | 说明 |
|------|---------|------|------|
| 背景故事 | `backstory` | `TEXT` | 调查员完整生平 |
| 外貌描述 | `appearance` | `TEXT` | 体貌特征描述 |
| 思想/信念 | `ideology_beliefs` | `TEXT` | 人生观、信仰 |
| 重要之人 | `significant_people` | `TEXT` | 对调查员有重要意义的人 |
| 意义非凡之地 | `meaningful_location` | `TEXT` | 有特殊情感联结的地点 |
| 宝贵之物 | `treasured_possession` | `TEXT` | 珍视的物品 |
| 特质 | `traits` | `TEXT` | 性格特点、口头禅等 |
| 伤口与疤痕 | `injuries_scars` | `TEXT` | 身体上的伤痕记录 |

---

## 10. 区块九：法术表

> 每项法术对应 `investigator_spells` 表的一行。

| 字段 | 数据库列 | 类型 | 必填 | 说明 |
|------|---------|------|------|------|
| 法术名 | `spell_name` | `VARCHAR(64)` | 🔵 | 法术名称 |
| 代价 | `cost` | `VARCHAR(64)` | ⚪ | 消耗（1D4 SAN / 5 MP / 1 POW 等） |
| 施法时间 | `casting_time` | `VARCHAR(32)` | ⚪ | 1轮 / 即时 / 1小时 等 |
| 效果 | `effect` | `TEXT` | ⚪ | 法术效果描述 |

---

## 11. 区块十：伙伴/关系表

> 每个伙伴对应 `investigator_companions` 表的一行。

| 字段 | 数据库列 | 类型 | 说明 |
|------|---------|------|------|
| 伙伴名 | `companion_name` | `VARCHAR(32)` | 伙伴角色名 |
| 玩家 | `player_name` | `VARCHAR(32)` | 扮演该伙伴的玩家 |
| 注释 | `notes` | `TEXT` | 关系描述 |
| 造成的改变 | `changes_made` | `TEXT` | 因伙伴而产生的改变 |
| 相遇模组 | `met_in_scenario` | `VARCHAR(128)` | 在哪场模组中相遇 |

---

## 12. 区块十一：疯狂/恐惧症/躁狂症

> 每项对应 `investigator_insanity` 表的一行。

| 字段 | 数据库列 | 类型 | 默认值 | 说明 |
|------|---------|------|--------|------|
| 类型 | `type` | `ENUM('phobia','mania','other')` | — | 恐惧症 / 躁狂症 / 其他 |
| 名称 | `name` | `VARCHAR(64)` | — | 如「幽闭恐惧症」「纵火狂」 |
| 描述 | `description` | `TEXT` | — | 症状详细描述 |

---

## 13. 区块十二：状态标记

### 13.1 当前 HP / SAN / MP (`hp_current` / `san_current` / `mp_current`)

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.hp_current` / `san_current` / `mp_current` |
| 业务含义 | 游戏进行中变化的当前值 |
| 数据类型 | `TINYINT UNSIGNED` |
| **校验规则** | `0 ≤ hp_current ≤ hp_max`  
`0 ≤ san_current ≤ san_max`  
`0 ≤ mp_current ≤ mp_max` |
| 默认值 | 创建人物卡时默认等于 `hp_max` / `san_max` / `mp_max` |
| 可编辑性 | ✏️ 创建时可覆盖；游戏进行中由系统更新 |
| 牵制关系 | → `hp_current ≤ hp_max/2` 时自动建议勾选「重伤」  
→ `hp_current = 0` 时自动建议勾选「濒死」  
→ `san_current` 单日损失 ≥5 时触发临时疯狂判断 |

### 13.2 克苏鲁神话 `cthulhu_mythos` / `cthulhu_mythos_current`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.cthulhu_mythos` / `cthulhu_mythos_current` |
| 业务含义 | 克苏鲁神话知识（初始值）→ 当前值 |
| 取值范围 | 0-99 |
| 默认值 | `0` |
| 校验 | `cthulhu_mythos_current ≥ cthulhu_mythos` |

### 13.3 状态 Toggle 开关（5 个）

| 字段 | 列 | 类型 | 业务含义 |
|------|-----|------|---------|
| 重伤 | `major_wound` | `BOOLEAN` | HP 降至一半以下触发 |
| 临时疯狂 | `temporary_insanity` | `BOOLEAN` | 单次检定大失败或 SAN 骤降触发 |
| 不定疯狂 | `indefinite_insanity` | `BOOLEAN` | 单日 SAN 损失 ≥1/5 最大值触发 |
| 濒死 | `dying` | `BOOLEAN` | HP=0 时触发 |
| 昏迷 | `unconscious` | `BOOLEAN` | 因伤害/药等原因失去意识 |

全部默认 `FALSE`，输入组件为 `<toggle>`。

### 13.4 经历包 `experience_pack`

| 属性 | 值 |
|------|-----|
| 数据库列 | `investigators.experience_pack` |
| 业务含义 | 是否启用「有故事的调查员」规则 |
| 取值 | `无` / `战场` / `警务` / `罪犯` / `医务` / `神话` |
| 默认值 | `无` |
| 输入组件 | `<select>` |
| 牵制关系 | → 选择非「无」时激活 `experience_points_total` 和 `experience_pts` 编辑 |

---

## 14. 附加一：跨字段牵制关系总图

```
                    ┌──────────────────────────────────┐
                    │         职业选择 (occupation_id)   │
                    └──────────────┬───────────────────┘
           ┌──────────────────────┼──────────────────────────┐
           ▼                      ▼                           ▼
    ┌──────────────┐    ┌──────────────────┐    ┌─────────────────────┐
    │ 本职技能列表  │    │ 信用评级范围       │    │ 职业点数公式        │
    │ (is_occ勾选)  │    │ (信用技能成功率)    │    │ (occupation_points) │
    └──────────────┘    └──────────────────┘    └──────────┬──────────┘
           │                                                │
           ▼                                                ▼
    ┌──────────────┐                              ┌──────────────────┐
    │ 职业点可编辑性 │                              │ 职业点数池总量    │
    │ (仅本职可编辑) │                              │ = EDU × 倍数     │
    └──────────────┘                              └──────────────────┘

                    ┌──────────────────────────────────┐
                    │        属性值变化                  │
                    └──────────────┬───────────────────┘
           ┌──────────┬──────────┬┼──────────┬──────────┬──────────┬──────────┐
           ▼          ▼          ▼▼          ▼          ▼          ▼          ▼
        HP_max    SAN_max    MP_max    DB/Build  闪避基础   母语基础   半值/1/5值
      =(CON+SIZ)  =POW     =POW/5    (查表)    =DEX/2    =EDU       =DIV
        /10                       (STR+SIZ)

                    ┌──────────────────────────────────┐
                    │        年龄变化                    │
                    └──────────────┬───────────────────┘
              ┌────────────────────┴────────────────────┐
              ▼                                         ▼
      EDU减值 (≥40岁每10年-5)                  MOV减值 (按年龄段)

                    ┌──────────────────────────────────┐
                    │      点数消耗实时校验               │
                    └──────────────┬───────────────────┘
    ┌──────────────────┬───────────────────┬──────────────────────┐
    ▼                  ▼                   ▼                      ▼
 经历包点            职业点              兴趣点              成长点
 SUM ≤ 池            SUM(本职) ≤ 池     SUM(全部) ≤ 池     无限制
(仅选包时激活)        (仅本职技能)        (所有技能)
```

---

## 15. 附加二：技能点完整分配流程

```
1. 选择职业 → 系统自动填充:
   ├─ 本职技能列表 (is_occupation = TRUE)
   ├─ 信用评级范围 (如 30-70)
   └─ 职业点数池 = EDU × 倍数

2. 填写属性 → 系统自动计算:
   ├─ 兴趣点数池 = INT × 2
   ├─ 经历包点数池（如选了包）
   └─ 所有技能基础值 + 闪避基础 = DEX/2 + 母语基础 = EDU

3. 【经历包点分配】→ SUM(experience_pts) ≤ experience_points_total
   仅经历包预设技能可分配

4. 【职业点分配】→ SUM(occupation_pts WHERE is_occ=TRUE) ≤ occupation_points_total
   仅本职技能可分配

5. 【兴趣点分配】→ SUM(interest_pts) ≤ interest_points_total
   所有技能均可分配

6. 信用评级技能 最终成功率 必须在职业给定范围内

7. ✅ 完美分配判定:
   occupation_points_used == occupation_points_total
   AND interest_points_used == interest_points_total
   AND (experience_points_used == experience_points_total OR experience_pack == "无")
   → 显示绿色完成标记
```

---

## 16. 附加三：年龄减值表

### EDU 减值

| 年龄段 | EDU 减值 | 实际 EDU |
|--------|---------|---------|
| 15-39 | 0 | 基础值 |
| 40-49 | -5 | 基础 -5 |
| 50-59 | -10 | 基础 -10 |
| 60-69 | -15 | 基础 -15 |
| 70-79 | -20 | 基础 -20 |
| 80-89 | -25 | 基础 -25 |

> 注：EDU 减值后下限为 15（但 CHECK 约束为 0-99，极端情况可低于 15）。

### MOV 减值

| 年龄段 | MOV 减值 |
|--------|---------|
| 15-39 | 0 |
| 40-49 | -1 |
| 50-59 | -2 |
| 60-69 | -3 |
| 70-79 | -4 |
| 80-89 | -5 |

> MOV 最终值还需叠加 STR/SIZ 差值减值 + DEX/SIZ 差值减值 + 护甲减值。

---

## 17. 附加四：DB / Build 查表

| STR + SIZ | 伤害加值 (DB) | 体格 (Build) |
|-----------|-------------|-------------|
| 2 ~ 64 | -2 | -2 |
| 65 ~ 84 | -1 | -1 |
| 85 ~ 124 | 0 | 0 |
| 125 ~ 164 | +1D4 | +1 |
| 165 ~ 204 | +1D6 | +2 |
| 205 ~ 284 | +2D6 | +3 |
| 285 ~ 364 | +3D6 | +4 |
| 365 ~ 444 | +4D6 | +5 |
| 445 ~ 524 | +5D6 | +6 |
| ... 每 +80 | 继续 +1D6 | 继续 +1 |

---

## 18. 附加五：信用评级范围速查

| 职业举例 | 信用范围 |
|---------|---------|
| 流浪者 / 罪犯 | 0 - 5 |
| 农民 / 劳工 | 0 - 10 |
| 店员 / 秘书 | 5 - 20 |
| 士兵 / 警察 | 9 - 30 |
| 记者 | 9 - 30 |
| 私家侦探 | 9 - 40 |
| 艺术家 / 作家 | 9 - 40 |
| 工程师 / 飞行员 / 牙医 | 30 - 60 |
| 医生 / 会计师 | 30 - 70 |
| 律师 | 20 - 80 |
| 电影演员 | 20 - 90 |
| 大企业 CEO | 50 - 99 |
| 自定义职业 | 0 - 99（用户自定） |

---

## 19. 附加六：技能基础值速查表

### 调查类

| 技能 | 基础值 |
|------|--------|
| 侦查 | 25% |
| 聆听 | 20% |
| 图书馆使用 | 20% |
| 追踪 | 10% |
| 导航 | 10% |
| 心理学 | 10% |
| 读唇 | 1% |

### 交涉类

| 技能 | 基础值 |
|------|--------|
| 取悦/魅惑 | 15% |
| 恐吓 | 15% |
| 说服 | 10% |
| 话术 | 5% |

### 战斗类

| 技能 | 基础值 |
|------|--------|
| 斗殴 | 25% |
| 闪避 | **DEX/2** |
| 投掷 | 20% |
| 手枪 | 20% |
| 来复枪 | 25% |
| 冲锋枪 | 15% |
| 机枪 | 10% |
| 弓术 | 15% |

### 特技类

| 技能 | 基础值 | 技能 | 基础值 |
|------|--------|------|--------|
| 攀爬 | 20% | 汽车驾驶 | 20% |
| 游泳 | 20% | 跳跃 | 20% |
| 潜行 | 20% | 骑术 | 5% |
| 妙手 | 10% | 锁匠 | 1% |
| 隐藏 | 10% | 乔装 | 5% |
| 电气维修 | 10% | 机械维修 | 10% |

### 学识类

| 技能 | 基础值 | 技能 | 基础值 |
|------|--------|------|--------|
| 母语 | **EDU** | 会计 | 5% |
| 估价 | 5% | 人类学 | 1% |
| 考古学 | 1% | 历史 | 5% |
| 法律 | 5% | 博物学 | 10% |
| 医学 | 1% | 急救 | 30% |
| 精神分析 | 1% | 科学 | 1% |
| 神秘学 | 5% | 外语 | 1% |
| 计算机使用 | 5% | 电子学 | 1% |
| 爆破 | 1% | 催眠 | 1% |

### 特殊

| 技能 | 基础值 | 说明 |
|------|--------|------|
| 信用评级 | 0% | 职业决定范围，不设固定基础值 |
| 克苏鲁神话 | 0% | 游戏中增长，初始通常 0 |
| 驯兽 | 5% | |
| 生存 | 10% | 按环境（沙漠/极地/海洋等） |
| 驾驶（船/飞行器） | 1% | |
| 重型机械 | 1% | |
| 潜水 | 1% | |
| 炮术 | 1% | |

---

## 20. 附加七：点数完美分配判定（J50 规则）

### 规则来源

COC 七版空白卡 CY21.1 人物卡 sheet J50 单元格。

### 业务规则

1. **职业点**和**兴趣点**同时归零（`pool - used = 0`）→ 视为完美分配，不提示任何警告
2. 只要有一个池还有剩余 → 显示剩余点数提醒
3. **经历包点**单独判断：选了经历包且还有剩余 → 额外提醒

### 前端实现逻辑

```javascript
function getPointSummary() {
  const occRemain = occTotal - occUsed;
  const intRemain = intTotal - intUsed;
  const expRemain = hasExpPack ? (expTotal - expUsed) : null;

  // 完美分配判定
  if (occRemain === 0 && intRemain === 0 && (expRemain === null || expRemain === 0)) {
    return { status: 'perfect', message: '✅ 点数已完美分配' };
  }

  // 剩余提醒
  const msgs = [];
  if (occRemain !== 0) msgs.push(`剩余职业点: ${occRemain}`);
  if (intRemain !== 0) msgs.push(`剩余兴趣点: ${intRemain}`);
  if (expRemain !== null && expRemain !== 0) msgs.push(`剩余经历包点: ${expRemain}`);

  return { status: 'warning', message: msgs.join('    ') };
}
```

### 对表单设计的影响

- **新增「经历包点」列**在技能表中（位于职业点之前）
- **点数池追踪**增为三个池（经历包 / 职业 / 兴趣）
- **完美分配**用绿色标记（而非之前仅校验不超限）
- **技能成功率公式**更新为 `base + experience + occupation + interest + growth`

---

> **文档结束**。本文档随 `schema.sql` 和 `COC人物卡字段分析.md` 共同维护。任何字段的业务语义变更、新增字段、或牵制关系调整，均需同步更新三份文件。
